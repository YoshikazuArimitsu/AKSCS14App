# docker-compose用の環境変数サンプル。
# コピーして .env にリネームし、実際のトークンを設定すること（.envはコミットしないこと）。
#
# cp .env.example .env

# LocalStack Pro のライセンストークン。
# AppHost側で使っているものと同じ値（例: dotnet user-secrets set "LocalStack:AuthToken" "<token>" --project src/AKSCS14App.AppHost）。
LOCALSTACK_AUTH_TOKEN=
